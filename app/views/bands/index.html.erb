<div class = 'container-fluid'>
  <div class = 'row'>
    <% @bands.each do |band| %>
      <div class='col-md-6'>
        <h3><%= link_to "#{band.name}", band_url(band)  %> </h3>
      </div>
    <% end %>
  </div>
</div>
